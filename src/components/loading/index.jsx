import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';
index.propTypes = {

};

function index(props) {
    return (
        <div>
            loading
        </div>
    );
}

export default index;